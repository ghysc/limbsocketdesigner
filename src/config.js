// Number of control points per spline curve
export const NUM_CONTROL_POINTS = 16;

// Default radius for new contours (mm)
export const DEFAULT_RADIUS_MM = 50;

// SDF grid resolution for socket generation
export const SDF_RESOLUTION = 32;

// Default slice heights (normalized 0-1, from extremity to top)
export const DEFAULT_SLICE_HEIGHTS = [0.25, 0.5, 0.75, 1.0];

// Default limb height (mm)
export const DEFAULT_LIMB_HEIGHT_MM = 200;
